---
import Header from "@/components/Header.astro";
import Footer from "@/components/Footer.astro";
import Breadcrumb from "@/components/Breadcrumb.astro";
import Layout from "@/layouts/Layout.astro";
import { SITE } from "@/config";
import { useTranslations, type Lang } from "@/i18n/ui";

export function getStaticPaths() {
  return [
    { params: { lang: "zh" } },
  ];
}

const { lang } = Astro.params as { lang: Lang };


// Content
const content = {
  title: "关于",
  intro: "这是一个基于 AstroPaper 主题构建的博客系统。",
  description: "AstroPaper 是一个简洁、无障碍且对 SEO 友好的博客主题，基于 Astro 和 TailwindCSS 构建。",
  features: "功能特点",
  featureList: [
    "🚀 **高性能** - 基于 Astro 5 的静态站点生成",
    "🌙 **明暗模式** - 支持明暗主题切换",
    "📱 **响应式设计** - 完美适配各种设备",
    "🔍 **SEO 友好** - 内置 SEO 优化",
    "♿ **无障碍** - 遵循 WCAG 标准",
  ],
  support: "支持项目",
  supportText: "如果你喜欢这个项目，欢迎给它一个 ⭐️",
};
---

<Layout title={`${content.title} | ${SITE.title}`}>
  <Header lang={lang} />
  <Breadcrumb lang={lang} />
  <main id="main-content" class="app-layout">
    <section id="about" class="app-prose mb-28 max-w-app prose-img:border-0">
      <h1 class="text-2xl tracking-wider sm:text-3xl">{content.title}</h1>
      
      <p>{content.intro}</p>
      <p>{content.description}</p>
      
      <h2>{content.features}</h2>
      <ul>
        {content.featureList.map(feature => (
          <li set:html={feature.replace(/\*\*(.*?)\*\*/g, '<strong>$1</strong>')} />
        ))}
      </ul>
      
      <h2>{content.support}</h2>
      <p>
        {content.supportText}
        <a href="https://github.com/scfast003/Astro-blog-cf" target="_blank" rel="noopener noreferrer">
          GitHub
        </a>
      </p>
    </section>
  </main>
  <Footer lang={lang} />
</Layout>

